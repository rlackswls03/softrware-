# FGSM 적대적 훈련의 강건성 일반화 한계 분석: 모델 간 전이 공격과 PGD 평가를 중심으로

## 1. 연구 개요

본 연구는 MNIST 이미지 분류 모델을 대상으로 FGSM 적대적 훈련이 실제로 어느 범위까지 강건성을 제공하는지 분석한다. 단순히 FGSM 공격을 재현하는 데서 멈추지 않고, 서로 다른 두 CNN 구조와 두 가지 학습 방식을 조합하여 네 개의 모델을 학습하고 비교하였다.

비교한 모델은 다음 네 가지다.

| 모델 이름 | 구조 | 학습 방식 |
|---|---|---|
| `lenet_standard` | LeNet | 일반 학습 |
| `smallcnn_standard` | SmallCNN | 일반 학습 |
| `lenet_fgsm_at` | LeNet | FGSM 적대적 훈련 |
| `smallcnn_fgsm_at` | SmallCNN | FGSM 적대적 훈련 |

핵심 연구 질문은 다음과 같다.

> FGSM 적대적 훈련으로 향상된 강건성이 다른 구조의 모델에서 생성된 전이 공격과 더 강한 반복 공격인 PGD에도 유지되는가?

## 2. 실험 설정

데이터셋은 MNIST를 사용하였다. 입력 이미지는 `[0, 1]` 픽셀 공간을 유지했으며, epsilon도 같은 공간에서 해석하였다. 기본 실험에서는 mean/std normalization을 적용하지 않았다.

주요 설정은 다음과 같다.

| 항목 | 값 |
|---|---|
| Dataset | MNIST |
| Seeds | 42, 123, 2026 |
| Epochs | 5 |
| Optimizer | Adam |
| Learning rate | 0.001 |
| FGSM 평가 epsilon | 0.00, 0.05, 0.10, 0.15, 0.20, 0.25, 0.30 |
| FGSM adversarial training epsilon | 0.25 |
| PGD 평가 | epsilon=0.25, steps=10, random start=True |
| 실행 장치 | CPU |

평가 지표는 clean accuracy, robust accuracy, clean accuracy retention, 조건부 전이 성공률을 사용하였다.

단, 현재 코드의 `attack_success_rate`는 `1 - robust_accuracy`로 계산된다. 이는 clean 이미지부터 틀렸던 표본까지 포함하므로 엄밀한 의미의 조건부 공격 성공률이라기보다 **overall adversarial error rate**에 가깝다. 따라서 보고서 해석에서는 전체 적대적 오분류율과 조건부 공격 성공률을 구분한다.

## 3. 핵심 결과

아래 표는 세 seed 평균 결과다.

| 모델 | Clean 정확도 | FGSM ε=0.25 robust 정확도 | PGD ε=0.25 robust 정확도 |
|---|---:|---:|---:|
| LeNet 일반 | 98.35% | 2.81% | 0.85% |
| SmallCNN 일반 | 99.25% | 28.70% | 0.94% |
| LeNet FGSM 훈련 | 97.52% | 87.57% | 15.39% |
| SmallCNN FGSM 훈련 | 99.19% | 96.65% | 10.07% |

FGSM 적대적 훈련은 같은 FGSM 공격에 대해 매우 큰 방어 효과를 보였다. LeNet은 FGSM robust 정확도가 2.81%에서 87.57%로 증가했고, SmallCNN은 28.70%에서 96.65%로 증가했다.

반면 clean 정확도 감소는 매우 작았다. LeNet은 98.35%에서 97.52%로 약 0.83%p 감소했고, SmallCNN은 99.25%에서 99.19%로 약 0.06%p 감소했다. 즉 본 실험에서는 정상 성능을 거의 희생하지 않으면서 동일한 FGSM 공격에 대한 강건성이 크게 향상되었다.

특히 `smallcnn_fgsm_at`은 FGSM ε=0.25 robust 정확도 96.65%를 기록하여, 군 적용 참고 자료에서 제시한 작전운용성능 참고 목표치 90%를 평균 기준으로 충족하였다. 다만 이 90% 값은 공식적·보편적 기준이 아니라 참고 목표치로만 해석해야 한다.

관련 그림:

- `results/figures/robustness_curve.png`
- `results/figures/clean_robust_comparison.png`
- `results/figures/clean_accuracy_retention.png`

## 4. 가장 중요한 발견: FGSM에만 강할 가능성

PGD 결과를 보면 FGSM 결과와 다른 양상이 나타난다. FGSM 적대적 훈련 모델은 동일한 FGSM 공격에는 높은 정확도를 보였지만, 반복 공격인 PGD에서는 강건성이 크게 떨어졌다.

방어 모델의 PGD seed별 robust 정확도는 다음과 같다.

| 모델 | Seed 42 | Seed 123 | Seed 2026 | 평균 ± 표준편차 |
|---|---:|---:|---:|---:|
| LeNet FGSM 훈련 | 2.91% | 40.26% | 3.01% | 15.39% ± 21.54%p |
| SmallCNN FGSM 훈련 | 29.81% | 0.08% | 0.31% | 10.07% ± 17.10%p |

즉 평균값만 보면 LeNet 방어 모델은 15.39%, SmallCNN 방어 모델은 10.07%로 요약되지만, 실제로는 seed에 따른 편차가 매우 크다. 특히 `smallcnn_fgsm_at`은 seed 42에서는 29.81%를 기록했지만, seed 123과 2026에서는 거의 0%에 가까운 PGD robust 정확도를 보였다.

이는 FGSM 적대적 훈련이 같은 단일 단계 공격에는 높은 정확도를 만들 수 있지만, 더 강한 반복적 first-order adversary인 PGD에는 안정적으로 일반화되지 않을 수 있음을 보여준다.

선행 연구에서는 이러한 현상을 FGSM adversarial training의 catastrophic overfitting 문제와 연결해 논의한다. 또한 true label을 사용해 FGSM 예제를 생성할 때 모델이 공격 패턴을 역이용하는 label leaking 현상도 알려져 있다. 본 실험 결과만으로 둘 중 하나라고 확정할 수는 없지만, FGSM에는 강하고 PGD에는 취약한 패턴은 두 가능성을 모두 의심하게 만든다.

관련 그림:

- `results/figures/pgd_whitebox.png`

## 5. FGSM 강도별 비단조 현상

일반적으로 공격 epsilon이 커질수록 robust 정확도는 낮아지는 것이 자연스럽다. 그러나 일부 방어 모델에서는 epsilon이 증가했는데도 robust 정확도가 오히려 상승하는 비단조 현상이 관찰되었다.

예를 들어 seed 42의 `lenet_fgsm_at`은 다음과 같은 결과를 보였다.

| 모델 | Seed | ε=0.05 | ε=0.20 |
|---|---:|---:|---:|
| LeNet FGSM 훈련 | 42 | 63.40% | 94.10% |

또한 seed 2026의 `smallcnn_fgsm_at`은 ε=0.20에서 99.39%를 기록했는데, 이는 해당 모델의 clean 정확도 99.22%보다도 높다.

| 모델 | Seed | ε=0.05 | ε=0.20 |
|---|---:|---:|---:|
| SmallCNN FGSM 훈련 | 2026 | 98.66% | 99.39% |

이 결과는 “공격 강도가 커질수록 더 잘 막았다”고 단순 해석하면 안 된다. 오히려 FGSM 방향이 실제 취약 방향을 제대로 찾지 못하거나, 모델이 특정 FGSM 생성 방식에 과적합했을 가능성을 보여준다. 이런 현상은 gradient masking 또는 label leaking 가능성을 검토해야 하는 중요한 단서다.

## 6. 전이성 분석

ε=0.25에서 평균 조건부 전이 성공률은 다음과 같다. outgoing은 해당 모델이 공격을 생성했을 때 다른 모델로 얼마나 잘 전이되는지를 의미하고, incoming은 다른 모델이 만든 공격을 받았을 때 얼마나 취약한지를 의미한다.

| 모델 | 공격 생성 능력 Outgoing | 공격을 받은 취약성 Incoming |
|---|---:|---:|
| LeNet 일반 | 35.16% | 21.91% |
| SmallCNN 일반 | 22.98% | 28.28% |
| LeNet FGSM 훈련 | 32.62% | 20.18% |
| SmallCNN FGSM 훈련 | 13.99% | 34.36% |

가장 흥미로운 모델은 `smallcnn_fgsm_at`이다. 이 모델은 자기 자신의 FGSM 공격에 대해서는 매우 강하게 보였지만, 다른 모델에서 생성된 공격에는 훨씬 취약했다.

주요 수치는 다음과 같다.

| 항목 | 값 |
|---|---:|
| `smallcnn_fgsm_at` 자기 자신의 FGSM 공격 성공률 | 약 2.72% |
| `lenet_standard`에서 생성한 공격의 `smallcnn_fgsm_at` 전이 성공률 | 약 45.46% |
| `smallcnn_fgsm_at`의 전체 incoming transfer 평균 | 34.36% |

즉 `smallcnn_fgsm_at`은 자기 gradient로 만든 FGSM에는 거의 완벽하게 버티지만, 다른 모델에서 생성된 공격에는 상당히 취약했다. 이는 실제 강건성이 전반적으로 높아서라기보다, 자기 모델의 gradient를 사용하는 FGSM이 취약점을 제대로 찾지 못했을 가능성을 시사한다.

PGD에서 seed에 따라 거의 무너지는 결과까지 함께 고려하면, FGSM 적대적 훈련 모델에 대해 gradient masking 또는 공격 방식 과적합 가능성을 추가 분석해야 한다.

관련 그림:

- `results/figures/transferability_eps_0.25.png`

## 7. 논의

초기 예상 결론은 “적대적 훈련을 적용하면 FGSM 방어 성능이 향상된다”였다. 실제 결과는 이보다 더 흥미롭다.

FGSM 적대적 훈련은 정상 정확도를 거의 유지하면서 동일한 FGSM 공격에 대한 강건성을 크게 향상시켰다. 그러나 PGD와 모델 간 전이 공격에서는 방어 효과가 안정적으로 일반화되지 않았다. 특히 seed에 따른 PGD 성능 편차와 epsilon별 비단조 robust 정확도는 단일 FGSM 공격에 대한 높은 정확도만으로 일반적인 적대적 강건성을 판단하기 어렵다는 점을 보여준다.

따라서 본 연구의 핵심 결론은 “FGSM 방어가 성공했다”가 아니라 다음과 같이 정리하는 것이 더 적절하다.

> FGSM 적대적 훈련은 정상 정확도를 거의 유지하면서 동일한 FGSM 공격에 대한 강건성을 크게 향상시켰다. 그러나 PGD와 모델 간 전이 공격에서는 방어 효과가 안정적으로 일반화되지 않았으며, 모델 구조와 초기 seed에 따라 성능 편차가 크게 나타났다. 따라서 단일 FGSM 공격에 대한 높은 정확도만으로 일반적인 적대적 강건성을 판단하기 어렵다.

## 8. 제출 전 보완하면 좋은 점

### 8.1 PGD 그래프 개선

현재 PGD 그래프는 모델별 평균 막대를 중심으로 보여준다. 그러나 본 실험에서 중요한 것은 평균보다 seed별 편차다. 따라서 PGD 그래프에는 다음 요소를 추가하는 것이 좋다.

- 모델별 평균 막대
- seed 42, 123, 2026 개별 점
- 표준편차 error bar

특히 다음 두 값을 그래프에 표시하면 결과의 핵심이 잘 드러난다.

| 모델 | PGD robust 정확도 |
|---|---:|
| LeNet FGSM 훈련 | 15.39% ± 21.54%p |
| SmallCNN FGSM 훈련 | 10.07% ± 17.10%p |

### 8.2 공격 성공률 용어 수정

현재 코드의 `attack_success_rate`는 `1 - robust_accuracy`이므로 clean 이미지부터 틀린 표본도 포함한다. 보고서에서는 이를 엄밀한 조건부 공격 성공률로 부르지 않는 것이 좋다.

권장 표현은 다음과 같다.

- `overall adversarial error rate`: 전체 평가 표본 중 adversarial example에서 오분류된 비율
- `conditional attack success rate`: clean 상태에서 맞힌 표본만 분모로 두었을 때 공격으로 오분류된 비율
- `conditional transfer success rate`: source와 target이 clean 상태에서 모두 맞힌 표본만 분모로 둔 전이 공격 성공률

### 8.3 PGD 추가 검증

현재 PGD는 ε=0.25, 10 steps, random start 1회로 평가하였다. 최종 제출 전에 다음 보조 실험을 추가하면 “PGD 설정이 약해서 나온 우연한 결과”라는 질문을 줄일 수 있다.

- 고정 테스트 표본 2,000개
- PGD-20
- random restart 5회
- 각 restart 중 가장 높은 loss를 만든 adversarial example 사용

PGD는 강건성 평가에서 대표적인 반복적 first-order adversary로 활용되므로, 이 추가 검증은 방어 모델의 일반화 한계를 설명하는 데 도움이 된다.

## 9. 결론

본 실험은 성공적으로 수행되었고, 결과도 단순한 재현을 넘어 의미 있는 분석 방향을 제공한다.

가장 중요한 결론은 다음과 같다.

> FGSM 방어는 같은 공격에는 강했지만, PGD와 전이 공격에서 공격 일반화 한계를 보였다.

따라서 본 프로젝트는 단순히 “FGSM adversarial training이 효과적이다”라고 결론 내리기보다, FGSM 적대적 훈련이 특정 공격 방식에는 과도하게 잘 맞을 수 있으며, 더 강한 반복 공격과 모델 간 전이 공격을 함께 평가해야 진짜 강건성을 판단할 수 있다는 점을 보여주는 연구로 정리하는 것이 적절하다.

## 10. 산출물 위치

주요 산출물은 다음 경로에 저장되어 있다.

| 항목 | 경로 |
|---|---|
| 자동 요약 보고서 | `results/summary.md` |
| 원본 CSV | `results/raw/` |
| 평균/표준편차 집계 CSV | `results/aggregated/` |
| 그래프 | `results/figures/` |
| 그림 인덱스 | `results/figures/figure_index.md` |
| 모델 checkpoint | `checkpoints/` |

