# 프로젝트 업데이트: Adversarial Firewall 확장

## 1. 왜 방향을 바꿨는가

기존 프로젝트는 MNIST 분류 모델 4개에 대해 FGSM, FGSM adversarial training, 전이 공격, PGD 평가를 수행했다. 핵심 결과는 FGSM adversarial training이 같은 FGSM 공격에는 매우 강하지만 PGD와 전이 공격에서는 안정적으로 일반화되지 않는다는 점이었다.

GPT와의 대화 맥락에서는 물리적 적대적 패치나 직접 촬영 이미지 실험이 교수님에게 너무 뻔하게 보일 수 있다는 점이 문제였다. 그래서 새 공격이나 시연을 붙이는 대신, 기존 결과를 바탕으로 **공격받은 입력을 탐지하고, 정화하고, 위험하면 거부하는 시스템 수준 방어**로 발전시키는 방향을 선택했다.

최종 확장 제목은 다음과 같이 잡았다.

> Adversarial Firewall: 적대적 입력 탐지·정화·거부 기반 군 AI 영상인식 방어 파이프라인

## 2. 이번에 구현한 핵심 기능

이번 확장은 기존 `scripts.run_pipeline`을 깨지 않고, 별도 firewall pipeline으로 추가했다.

구현된 핵심 기능은 다음 4개다.

| 기능 | 구현 상태 | 설명 |
|---|---:|---|
| MNIST convolutional autoencoder | 완료 | clean MNIST 이미지를 복원하는 purifier/reformer |
| Reconstruction error detector | 완료 | `MSE(x, AE(x))`가 threshold보다 크면 적대적 입력 의심 |
| Purify and reclassify | 완료 | 공격 입력을 autoencoder로 정화한 뒤 classifier로 재분류 |
| Reject option policy | 완료 | 위험 점수가 높고 정화 후 confidence가 낮으면 `REJECT_SUSPICIOUS` 처리 |

추가로 연구 결과처럼 보이게 만드는 시각화와 요약 기능도 구현했다.

| 기능 | 구현 상태 | 설명 |
|---|---:|---|
| Detection ROC/AUC | 완료 | reconstruction error 기반 clean vs attack 탐지 ROC |
| TPR@FPR 5% | 완료 | clean 오탐률 5% 기준 탐지율 |
| score distribution plot | 완료 | clean/FGSM/PGD reconstruction error 분포 |
| accuracy recovery plot | 완료 | original accuracy, purified accuracy, final safe accuracy 비교 |
| decision breakdown plot | 완료 | 원본 통과/정화 후 통과/거부 비율 |
| image grid | 완료 | original / attacked input / purified 이미지 비교 |
| summary.md 자동 확장 | 완료 | `results/summary.md`에 Firewall 섹션 자동 추가 |
| visual report | 완료 | 기존 checkpoint/CSV/PNG를 읽어 발표용 HTML과 storyboard PNG 생성 |

## 3. 새로 추가한 주요 파일

### 소스 코드

- `src/adversarial_mnist/autoencoder.py`
  - `ConvAutoencoder`
  - autoencoder checkpoint 저장/로드 helper
  - reconstruction loss helper

- `src/adversarial_mnist/firewall.py`
  - `reconstruction_error`
  - `choose_threshold_from_clean_validation`
  - `firewall_decision`
  - `ACCEPT_ORIGINAL`
  - `ACCEPT_PURIFIED`
  - `REJECT_SUSPICIOUS`
  - ROC/AUC helper

- `src/adversarial_mnist/firewall_evaluation.py`
  - threshold calibration
  - Clean/FGSM/PGD firewall evaluation
  - 샘플별 detection score 생성
  - 조건별 firewall result 생성
  - detection AUC summary 생성

### 실행 스크립트

- `scripts/train_autoencoder.py`
  - clean MNIST train set으로 autoencoder 학습
  - checkpoint 저장: `checkpoints/autoencoder_seed{seed}.pt`
  - history 저장: `results/raw/autoencoder_history.csv`

- `scripts/evaluate_firewall.py`
  - `smallcnn_standard`, `smallcnn_fgsm_at` 대상 firewall 평가
  - Clean, FGSM eps=0.25, PGD eps=0.25 조건 평가

- `scripts/plot_firewall_results.py`
  - firewall CSV를 읽어 그래프 생성
  - `results/summary.md`의 Firewall 섹션 업데이트

- `scripts/run_firewall_pipeline.py`
  - autoencoder 학습
  - threshold calibration
  - firewall 평가
  - 그래프 생성
  - summary 업데이트를 한 번에 실행

- `scripts/build_visual_report.py`
  - 이미 학습된 checkpoint와 저장된 결과 CSV/PNG를 사용해 발표용 정적 HTML 생성
  - 새 학습 없이 `results/visual_report/index.html`과 storyboard PNG 생성

### 시각 보고서 모듈

- `src/adversarial_mnist/visual_report.py`
  - `training_progress.png`
  - `autoencoder_training_progress.png`
  - `project_pipeline_flow.png`
  - `result_storyboard.png`
  - `results/visual_report/index.html`
  - checkpoint inventory table 생성

### 테스트

- `tests/test_autoencoder.py`
  - autoencoder output shape
  - output range `[0, 1]`
  - reconstruction loss scalar 확인

- `tests/test_firewall.py`
  - reconstruction error shape/value
  - threshold selection
  - reject policy decision
  - batch decision
  - AUC와 TPR@FPR 계산

## 4. 수정한 기존 파일

- `configs/default.json`
  - `firewall` 설정 추가
  - 기본 평가 모델: `smallcnn_standard`, `smallcnn_fgsm_at`
  - default autoencoder epoch: 5
  - firewall quick autoencoder epoch: 10
  - firewall quick PGD step: 3

- `src/adversarial_mnist/utils.py`
  - quick override가 `firewall` 섹션에도 적용되도록 수정

- `src/adversarial_mnist/visualization.py`
  - firewall 전용 그래프 함수 추가
  - firewall summary 자동 생성 함수 추가
  - figure index에 firewall 그래프 설명 추가

- `README.md`
  - Adversarial Firewall 확장 설명 추가
  - quick/full 실행 명령 추가
  - firewall 결과 파일 설명 추가
  - CPU full 실행이 오래 걸릴 수 있다는 주의 추가

- `paper/paper_outline.md`
  - Part 2: Adversarial Firewall 확장 추가
  - 논의/한계에 detection, purification, reject option 해석 추가
  - adaptive attack에 대한 한계 명시

## 5. 새 실행 명령

가장 중요한 명령은 이것이다.

```bash
python -m scripts.run_firewall_pipeline --quick --force
```

중간 규모 검증은 다음처럼 실행할 수 있다.

```bash
python -m scripts.run_firewall_pipeline --force --seeds 42 --test-subset 2000 --pgd-steps 3
```

개별 단계 실행도 가능하다.

```bash
python -m scripts.train_autoencoder --config configs/default.json --seeds 42 --force
python -m scripts.evaluate_firewall --config configs/default.json --seeds 42 --force
python -m scripts.plot_firewall_results --config configs/default.json
```

발표용 시각 보고서 생성:

```bash
python -m scripts.build_visual_report --config configs/default.json
```

## 6. 생성된 결과 파일

현재 full firewall pipeline 실행으로 다음 파일들이 생성됐다.

| 파일 | 설명 |
|---|---|
| `checkpoints/autoencoder_seed42.pt` | autoencoder purifier checkpoint |
| `results/raw/autoencoder_history.csv` | autoencoder 학습 loss |
| `results/raw/firewall_detection_scores.csv` | 샘플별 reconstruction error, decision, prediction |
| `results/raw/firewall_results.csv` | 조건별 accuracy, detection rate, reject rate |
| `results/raw/firewall_examples.pt` | 이미지 시각화용 tensor |
| `results/aggregated/firewall_detection_summary.csv` | AUC, TPR@FPR 5% |
| `results/aggregated/firewall_results_summary.csv` | firewall 결과 요약 |
| `results/figures/firewall_score_distribution.png` | detection score 분포 |
| `results/figures/firewall_detection_roc_curve.png` | ROC curve |
| `results/figures/firewall_accuracy_recovery.png` | 정화 전후 accuracy 비교 |
| `results/figures/firewall_decision_breakdown.png` | decision 비율 |
| `results/figures/original_attacked_purified_examples.png` | original/input/purified 이미지 grid |
| `results/visual_report/index.html` | 브라우저에서 열어보는 발표용 시각 보고서 |
| `results/figures/project_pipeline_flow.png` | 전체 실험 흐름도 |
| `results/figures/training_progress.png` | CNN 모델 학습 경과 |
| `results/figures/autoencoder_training_progress.png` | autoencoder purifier 학습 경과 |
| `results/figures/result_storyboard.png` | FGSM, PGD, Firewall 핵심 결과 한 장 요약 |

## 7. 현재 full 실행 결과 요약

아래 수치는 `seed=42`, test 10,000개, FGSM eps=0.25, PGD eps=0.25, PGD 10 step 기준으로 실행한 full firewall 결과다.

| 모델 | 조건 | Original accuracy | Purified accuracy | Detection rate | Reject rate | Final safe accuracy |
|---|---|---:|---:|---:|---:|---:|
| smallcnn_standard | Clean | 99.24% | 98.71% | 3.78% | 0.15% | 99.03% |
| smallcnn_standard | FGSM | 22.85% | 68.55% | 100.00% | 21.49% | 80.76% |
| smallcnn_standard | PGD | 0.95% | 79.44% | 100.00% | 12.80% | 85.85% |
| smallcnn_fgsm_at | Clean | 99.20% | 98.52% | 3.78% | 0.20% | 98.96% |
| smallcnn_fgsm_at | FGSM | 96.16% | 93.98% | 100.00% | 6.80% | 96.89% |
| smallcnn_fgsm_at | PGD | 29.63% | 81.98% | 100.00% | 13.63% | 88.62% |

Detection summary quick 결과:

| 모델 | Attack condition | AUC | TPR@FPR 5% |
|---|---|---:|---:|
| smallcnn_standard | FGSM | 1.000 | 100.00% |
| smallcnn_standard | PGD | 1.000 | 100.00% |
| smallcnn_standard | ALL_ATTACKS | 1.000 | 100.00% |
| smallcnn_fgsm_at | FGSM | 1.000 | 100.00% |
| smallcnn_fgsm_at | PGD | 1.000 | 100.00% |
| smallcnn_fgsm_at | ALL_ATTACKS | 1.000 | 100.00% |

이 결과는 단일 seed 42 기준이므로 여러 seed 평균처럼 해석하면 안 된다. 다만 quick 연결 검증이 아니라 full test set 10,000개에서 생성된 실제 firewall 평가 결과다.

## 8. 검증한 명령과 결과

성공한 검증:

```bash
ruff check .
pytest -q
python -m scripts.run_firewall_pipeline --quick --force --no-progress
python -m scripts.run_firewall_pipeline --force --seeds 42 --no-progress
python -m scripts.build_visual_report --config configs/default.json
python -m scripts.build_simulation --config configs/default.json
```

결과:

- `ruff check .`: 통과
- `pytest -q`: 21개 테스트 통과
- `run_firewall_pipeline --quick --force`: 성공
- firewall CSV/그래프/summary.md 생성 성공
- `run_firewall_pipeline --force --seeds 42`: 성공
- full firewall 실행 시간: 약 12분 52초, CPU 환경
- `build_visual_report`: 성공
- `build_simulation`: 성공

## 9. 구현 범위 평가

MVP 요구사항 기준 구현률:

| 항목 | 상태 |
|---|---|
| Autoencoder 모델 | 완료 |
| Autoencoder 학습 스크립트 | 완료 |
| Reconstruction error detector | 완료 |
| Clean validation FPR 5% threshold | 완료 |
| Clean/FGSM/PGD 평가 | 완료 |
| `smallcnn_standard`, `smallcnn_fgsm_at` 우선 평가 | 완료 |
| Reject option policy | 완료 |
| 결과 CSV 저장 | 완료 |
| 그래프 저장 | 완료 |
| `summary.md` Firewall 섹션 자동 추가 | 완료 |
| README/paper outline 반영 | 완료 |
| visual report HTML 생성 | 완료 |
| interactive simulation HTML 생성 | 완료 |
| 발표용 storyboard PNG 생성 | 완료 |
| 테스트 추가 | 완료 |
| quick firewall pipeline 성공 | 완료 |
| full seed 42 firewall pipeline 성공 | 완료 |

의도적으로 제외한 것:

- Streamlit dashboard
- 실시간 웹 서버 기반 dashboard
- physical adversarial patch
- 직접 촬영 이미지
- transfer attack까지 firewall 전체 적용
- adaptive attack 대응
- feature squeezing/confidence drop/prediction disagreement detector 다중 비교

이 항목들은 이번 MVP 범위를 넘어가므로 향후 연구 또는 선택 확장으로 남기는 것이 낫다.

## 10. GPT와 대화할 때 사용할 요약

아래 문단을 GPT에게 그대로 붙여넣으면 된다.

```text
기존 MNIST FGSM/PGD/전이성 프로젝트를 Adversarial Firewall MVP로 확장했다. 교수님이 물리 패치나 사진 실험을 뻔하게 볼 수 있다는 맥락 때문에, 새 공격을 추가하는 대신 기존 결과에서 드러난 FGSM adversarial training의 PGD 일반화 한계를 바탕으로 입력 단계 방어 파이프라인을 만들었다.

구현한 것은 convolutional autoencoder purifier, reconstruction error 기반 detector, clean validation FPR 5% threshold calibration, purify-and-reclassify, reject option policy다. 평가 대상은 우선 smallcnn_standard와 smallcnn_fgsm_at 두 모델로 제한했다. 입력 조건은 Clean, FGSM eps=0.25, PGD eps=0.25다.

새 파일은 src/adversarial_mnist/autoencoder.py, firewall.py, firewall_evaluation.py, visual_report.py, simulation.py, scripts/train_autoencoder.py, scripts/evaluate_firewall.py, scripts/plot_firewall_results.py, scripts/run_firewall_pipeline.py, scripts/build_visual_report.py, scripts/build_simulation.py, tests/test_autoencoder.py, tests/test_firewall.py다.

quick firewall pipeline은 성공했고, results/raw/firewall_detection_scores.csv, results/raw/firewall_results.csv, results/figures/firewall_score_distribution.png, firewall_detection_roc_curve.png, firewall_accuracy_recovery.png, firewall_decision_breakdown.png, original_attacked_purified_examples.png가 생성됐다. results/summary.md에도 Adversarial Firewall 섹션이 자동 추가된다.

full seed 42 firewall pipeline까지 성공했고, results/raw/firewall_detection_scores.csv, results/raw/firewall_results.csv, results/figures/firewall_score_distribution.png, firewall_detection_roc_curve.png, firewall_accuracy_recovery.png, firewall_decision_breakdown.png, original_attacked_purified_examples.png가 생성됐다. results/summary.md에도 Adversarial Firewall 섹션이 자동 추가된다.

추가로 발표용 시각 보고서도 구현했다. `python -m scripts.build_visual_report --config configs/default.json`를 실행하면 `results/visual_report/index.html`이 생성되고, 기존 학습 결과와 Firewall 결과를 project flow, training progress, result storyboard, image grid 형태로 볼 수 있다. 새 학습을 수행하지 않고 이미 저장된 checkpoint/CSV/PNG를 재사용한다.

추가로 시뮬레이션형 시각화도 구현했다. `python -m scripts.build_simulation --config configs/default.json`를 실행하면 `results/simulation/index.html`이 생성된다. 이 HTML은 `results/raw/firewall_examples.pt`와 `results/raw/firewall_results.csv`를 읽어 원본 입력 -> 공격 입력 -> autoencoder 정화 -> reconstruction-error 탐지 -> 최종 accept/purify/reject 판정을 단계별로 재생한다. Clean/FGSM/PGD 조건과 샘플을 바꿀 수 있고, reconstruction score와 threshold 게이지, full-test metric도 함께 표시한다. 새 학습이나 새 평가를 수행하지 않고 기존 결과를 발표용으로 재생하는 도구다.

주의할 점은 현재 full firewall 결과가 seed 42 단일 seed 결과라는 것이다. 기존 FGSM/PGD/전이성 본 실험은 seed 42, 123, 2026 평균이지만, firewall 확장은 seed 42 full test set 기준이다. 따라서 보고서에는 “Firewall 확장은 seed 42 기준의 추가 시스템 검증”이라고 표시하는 것이 안전하다.
```

## 11. 보고서에 넣기 좋은 해석 문장

기존 실험에서 FGSM adversarial training은 FGSM 공격에는 높은 robust accuracy를 보였지만, PGD와 전이 공격에서는 일반화 한계를 보였다. 이에 본 확장에서는 모델 자체를 더 강건하게 만드는 접근을 넘어서, 입력 단계에서 적대적 입력을 탐지하고 autoencoder로 정화한 뒤, 위험도가 높으면 판단을 거부하는 Adversarial Firewall을 구현했다.

이 확장은 새로운 방어 알고리즘을 제안한다기보다, 공격 평가 프로젝트를 시스템 수준 방어 파이프라인으로 발전시킨 것이다. 특히 군 AI 적용 맥락에서는 모든 입력을 무조건 자동 분류하는 것보다, 위험 입력을 탐지하고 정화하거나 인간 검토로 넘기는 reject option이 실용적인 방어 절차로 해석될 수 있다.

단, 이 prototype은 adaptive attack에 대한 보장을 제공하지 않는다. 공격자가 autoencoder와 detector 구조를 알고 최적화할 경우 reconstruction error 기반 탐지는 우회될 수 있다. 따라서 본 결과는 완전한 보안 해법이 아니라, 입력 단계 탐지·정화·거부 절차의 가능성을 확인하는 MVP로 해석해야 한다.
